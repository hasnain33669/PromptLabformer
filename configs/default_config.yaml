model:
  node_dim: 145
  hidden_dim: 256
  num_heads: 8
  num_layers: 3
  num_prompts: 5
  prompt_dim: 256
  dropout: 0.1
  edge_dim: 12
  descriptor_dim: 11

training:
  batch_size: 64
  learning_rate: 0.0001
  weight_decay: 0.0001
  epochs: 150
  warmup_steps: 1000

data:
  dataset: 
  test_size: 0.1
  val_size: 0.1

device: cuda
seed: 42
